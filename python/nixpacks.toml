[phases.setup]
nixPkgs = [
  "python312",
  "ffmpeg",
  "cairo",
  "pango",
  "gdk-pixbuf",
  "glib",
  "libffi",
  "harfbuzz",
  "fontconfig"
]

[phases.build]
cmds = [
  "pip install --upgrade pip && pip install -r requirements.txt"
]

[start]
cmd = "python main.py"
