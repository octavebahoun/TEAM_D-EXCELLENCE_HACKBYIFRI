/**
 * @file socket.js
 * @description Configuration avancée du serveur WebSocket (Socket.io).
 * Définit les options de connexion, les middlewares Socket.io,
 * les paramètres CORS, les namespaces et la gestion
 * des événements de connexion/déconnexion des clients.
 */
const cors = require('cors');